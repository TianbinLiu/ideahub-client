# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
.env

# ── Claude Code ─────────────────────────────────────────────────────────
# .claude/skills/ 是【团队共享】配置，必须入库——新成员克隆后就带着工程规则 skill。
# 只挡本机工作区与可能含个人 token 的本地设置覆盖。
.claude/worktrees/
.claude/settings.local.json

# 本机专用的 Vite 模式覆盖（如 .env.e2e.local：本地端到端联调指向本机 server）
.env.*.local
